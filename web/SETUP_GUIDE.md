# 🚀 论文生成系统 - 完整设置指南

## 📋 快速检查清单

在启动系统前，请确保：

- ✅ 所有文件完整
- ✅ HTTP服务器正确配置
- ✅ 模板文件可访问
- ✅ 浏览器支持现代JavaScript

## 🔧 启动步骤

### 第1步：验证文件结构

确保您的目录结构如下：

```
web/
├── index.html
├── templates/
│   ├── outline.template.json
│   └── content.template.json
├── css/
│   ├── main.css
│   └── editor.css
├── js/
│   ├── app.js
│   ├── project.js
│   ├── outline.js
│   ├── content.js
│   ├── executor.js
│   └── utils.js
├── lib/
│   └── jszip.min.js
└── start-server.sh
```

**验证命令**:
```bash
cd /Users/pc/Documents/promptx_tools/web
ls -la templates/
# 应该看到：outline.template.json 和 content.template.json
```

### 第2步：启动HTTP服务器

**方法1: 使用启动脚本**
```bash
./start-server.sh
```

**方法2: 手动启动**
```bash
# Python 3
python3 -m http.server 8080

# 或 Python 2
python -m SimpleHTTPServer 8080

# 或 Node.js (需安装http-server)
npx http-server -p 8080
```

### 第3步：验证服务器

在浏览器中访问以下URL来验证：

1. **主页**: http://localhost:8080/
2. **模板文件**: http://localhost:8080/templates/outline.template.json
3. **JavaScript文件**: http://localhost:8080/js/app.js

**所有请求都应该返回200状态，不应该看到404错误**

### 第4步：打开系统

访问：http://localhost:8080

检查浏览器控制台：
- ✅ 应该看到：`✅ 模板加载成功`
- ❌ 如果看到错误，请参考下面的故障排除

## 🐛 常见问题排除

### 问题1: 模板加载失败 (404错误)

**症状**:
- `Failed to load resource: 404 (File not found)`
- `SyntaxError: Unexpected token '<'`

**原因**:
- templates目录不存在或为空
- HTTP服务器路径配置错误

**解决方案**:
```bash
# 检查模板文件是否存在
ls -la web/templates/

# 如果不存在，复制模板文件
cp -r templates web/

# 重新启动服务器
cd web
python3 -m http.server 8080
```

### 问题2: JavaScript语法错误

**症状**:
- `SyntaxError: Unexpected token`
- 页面功能不正常

**解决方案**:
```bash
# 验证JavaScript语法
cd web
node -c js/app.js
node -c js/project.js
# 应该没有输出，表示语法正确
```

### 问题3: 端口被占用

**症状**:
- `Address already in use`

**解决方案**:
```bash
# 查找占用进程
lsof -i :8080

# 杀死进程
kill <PID>

# 或使用其他端口
python3 -m http.server 8081
```

### 问题4: 权限问题

**症状**:
- `Permission denied`

**解决方案**:
```bash
# 给启动脚本执行权限
chmod +x start-server.sh

# 检查文件权限
ls -la web/
```

## 📱 浏览器要求

### 支持的浏览器
- ✅ Chrome 80+
- ✅ Firefox 75+
- ✅ Safari 13+
- ✅ Edge 80+

### 必需的功能
- JavaScript ES6+
- Fetch API
- localStorage
- File API

### 检查方法
在浏览器控制台执行：
```javascript
// 检查Fetch API
console.log(typeof fetch); // 应该输出 "function"

// 检查localStorage
console.log(typeof localStorage); // 应该输出 "object"

// 检查File API
console.log(typeof FileReader); // 应该输出 "function"
```

## 🔍 调试技巧

### 控制台调试
1. 按F12打开开发者工具
2. 查看Console选项卡的错误信息
3. 查看Network选项卡检查文件加载状态
4. 查看Sources选项卡调试JavaScript

### 网络调试
```bash
# 测试模板文件可访问性
curl -I http://localhost:8080/templates/outline.template.json
# 应该返回 HTTP/1.0 200 OK

# 测试JSON格式
curl http://localhost:8080/templates/outline.template.json | head -3
# 应该看到JSON内容，不是HTML
```

### 日志级别
在浏览器控制台中设置详细日志：
```javascript
// 启用详细日志
localStorage.setItem('debug', 'true');
location.reload();
```

## 🎯 验证系统正常工作

当系统正确启动后，您应该看到：

1. **主页面**: 显示项目管理界面，有6个导航按钮
2. **控制台输出**:
   ```
   📄 论文生成系统 v1.0.0
   🚀 系统初始化完成
   正在加载模板文件...
   ✅ 模板加载成功
   ✅ 应用初始化成功
   ```
3. **无错误**: Network和Console选项卡没有红色错误信息

## 📞 获取帮助

如果遇到问题：

1. **检查**: 确保所有文件都在正确位置
2. **重启**: 重新启动HTTP服务器
3. **清理**: 清除浏览器缓存并刷新
4. **日志**: 查看浏览器控制台的具体错误信息

**成功标志**: 当看到"✅ 模板加载成功"时，系统就可以正常使用了！

---

**提示**: 如果仍有问题，请确保从 `/Users/pc/Documents/promptx_tools/web` 目录启动服务器，并验证所有模板文件都存在且格式正确。