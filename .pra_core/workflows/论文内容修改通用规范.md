# 📚 论文内容修改通用规范

**版本**: 1.0
**适用**: 所有基于真实项目的毕业论文
**目标**: 将论文从技术文档风格转变为学术论文风格，确保技术准确性

---

## 🎯 核心原则

### 1. 真实性第一原则

**铁律**：论文内容必须100%基于项目实际实现，严禁虚构。

**检查方法**：
- 对照项目的 `source-truth.json` 文件
- 检查 `implementedFeatures` 列表
- 核对 `prohibitedTechnologies` 禁用列表

**示例**：
```
❌ 错误：系统使用Kubernetes进行容器编排
✅ 正确：系统支持服务的弹性伸缩（如果source-truth.json中Kubernetes在禁用列表）
```

---

### 2. 层次分明原则

**章节定位**：
- **设计章节**（如第4章）= 总述 + 图表（轻量级）
- **实现章节**（如第5章）= 方案级描述（详细但不过度）
- **测试章节**（如第6章）= 概述 + 表格（极简）

---

### 3. 风格转换原则

**从"技术文档"到"论文实现"**：
- 代码级 → 方案级
- 具体参数 → 概括描述
- 编程语法 → 技术方案

---

## 📖 分章节修改规范

### 第二章：技术基础

#### ✅ 应该写什么
- 系统实际使用的技术栈
- 技术的基本概念和特点
- 技术选型的理由

#### ❌ 不应该写什么
- 与项目无关的技术理论
- 空泛的行业特点（如"电商系统技术特点"）

#### 📝 修改示例
```
❌ 2.8 电商系统技术特点（空泛，不是技术介绍）
✅ 2.8 Vue前端框架（项目实际使用）
```

---

### 第三章：需求分析

#### ✅ 应该写什么
- 系统的功能需求（基于实际功能）
- 非功能需求（基于实际性能指标）

#### ❌ 不应该写什么
- 未实现的技术需求

#### 📝 修改示例
```
❌ 数据库支持分库分表，应对数据量快速增长
✅ 删除（如果项目未实现分库分表）

❌ 系统遵循领域驱动设计方法
✅ 系统参考领域驱动设计方法（更准确）
```

---

### 第四章：系统设计

#### ✅ 应该写什么
- 架构设计思路
- 模块设计概述
- 设计图表

#### ❌ 不应该写什么
- 详细的实现细节
- 具体的配置参数
- 代码级的说明

#### 📝 修改规则

**4.2 数据库设计**：
- 4.2.2 逻辑结构设计：
  ```
  ✅ 保留：开头总述
  ✅ 保留：每个表的ER图
  ✅ 简化：每个表只一句话描述
  ❌ 删除：详细字段列举
  ```

- 4.2.3 表结构设计：
  ```
  ✅ 保留：详细字段说明
  ✅ 保留：主键、外键、数据类型说明
  ❌ 删除：索引设计说明
  ```

**4.4 微服务治理设计**：
```
✅ 保留：第一段总述
✅ 保留：治理架构图
❌ 删除：第二段及之后的详细描述（移到第5章）
```

**4.5 高并发保障设计**：
```
✅ 保留：第一段总述
✅ 保留：高并发方案图
❌ 删除：第二段及之后的详细描述（移到第5章）
```

---

### 第五章：系统实现

#### ✅ 应该写什么
- 使用的技术框架
- 实现的核心功能
- 解决的技术问题

#### ❌ 不应该写什么
- 具体的注解名称
- 具体的类名和方法名
- 具体的配置参数值
- 具体的SQL代码
- 具体的接口路径

#### 📝 风格转换对照表

| 类型 | ❌ 技术文档风格 | ✅ 论文风格 |
|------|----------------|------------|
| 注解 | 使用@Async注解标注异步方法 | 使用异步注解标注异步方法 |
| 类名 | UserMapper继承BaseMapper | 数据访问接口继承基础接口 |
| 参数 | 配置最小连接数10、最大连接数50 | 配置合理的连接数和超时参数 |
| SQL | SELECT * FROM user WHERE phone = #{phone} | 根据手机号查询用户 |
| 命令 | 使用HGETALL命令获取购物车 | 获取购物车数据 |
| 路径 | POST /api/user/register | 注册接口 |
| 数值 | 设置过期时间为7天 | 设置合理的过期时间 |

#### 📝 删除示例

**异步处理段落：**
```
❌ 删除：
使用@Async注解标注异步方法，在配置类上添加@EnableAsync启用异步支持。
配置线程池参数，设置核心线程数、最大线程数、队列容量等。
异步方法返回Future或CompletableFuture。

✅ 改为：
使用异步注解标注异步方法，配置线程池参数。
异步方法支持返回异步结果对象。
```

**数据库优化段落：**
```
❌ 删除：
为商品表的category_id、price、sales字段建立索引。
避免WHERE DATE(create_time) = '2024-01-01'，
改为WHERE create_time >= '2024-01-01 00:00:00' AND create_time < '2024-01-02 00:00:00'。
配置最小连接数10、最大连接数50、连接超时时间30秒。

✅ 改为：
为高频查询字段建立索引。
查询优化避免在条件中使用函数，保证索引生效。
数据库连接池配置合理的连接数和超时参数。
```

---

### 第六章：系统测试

#### ✅ 应该写什么
- 测试环境概述
- 测试策略说明
- 测试结论

#### ❌ 不应该写什么
- 详细的测试步骤
- 具体的测试数据
- 具体的测试参数值

#### 📝 修改模板

**6.1 测试环境与策略**：
```
结构：
  第一段：测试概述
  第二段：测试环境配置（删除具体硬件参数）
  ❌ 删除：第三段及之后的详细描述
```

**6.2.x 各功能测试**：
```
模板：
  XX功能测试验证[功能1]、[功能2]、[功能3]等功能的正确性。
  详细测试用例如表X-Y所示。

精简比例：95%以上
```

**6.3 性能测试**：
```
结构：
  第一段：测试工具和目标
  第二段：测试环境和场景（删除具体参数）
  第三段：测试结论（删除具体数据）
  ❌ 删除：第三到倒数第二段的所有详细数据
```

---

### 第七章：结语

#### ✅ 应该写什么
- 实际完成的成果
- 真实使用的技术
- 实际解决的问题

#### ❌ 不应该写什么
- 未实现的技术
- 过度的技术承诺

#### 📝 修改示例
```
❌ 数据库优化通过索引设计、SQL优化、连接池优化、读写分离等手段
✅ 数据库优化通过索引设计、SQL优化、连接池优化等手段
（删除"读写分离"，因为未确认实现）
```

---

## 🔍 技术准确性检查清单

### 步骤1：读取source-truth.json

必须字段：
- `implementedFeatures`: 已实现功能列表
- `prohibitedTechnologies`: 禁用技术列表
- `microservices`: 微服务组件配置
- `backend`: 后端技术栈
- `frontend`: 前端技术栈

---

### 步骤2：逐章检查技术描述

**检查项目：**

1. **搜索禁用技术**
   ```bash
   grep -i "kubernetes\|docker\|elasticsearch\|kafka" chapters/*.json
   ```
   如果找到，全部删除或泛化。

2. **搜索未确认技术**
   ```
   未在source-truth.json中的技术：
   - 读写分离、主从复制
   - 分库分表
   - 对象存储（OSS、S3）
   - 监控工具（Prometheus、Grafana、ELK）
   - 复杂算法（如果未实现）
   ```
   找到后删除或改为泛化描述。

3. **验证implementedFeatures**
   ```
   论文中提到的功能必须在此列表中：
   - 用户注册登录认证 ✅
   - 商品分类管理 ✅
   - 订单管理 ✅
   - 购物车功能 ✅
   - ...
   ```

---

### 步骤3：技术泛化对照表

| 具体技术 | 泛化表述 | 使用场景 |
|---------|---------|---------|
| Kubernetes | 容器编排平台 | 未确认时 |
| Elasticsearch | 搜索引擎 / 删除 | 禁用时 |
| Prometheus+Grafana | 监控系统、可视化平台 | 未确认时 |
| ELK | 集中式日志方案 | 未确认时 |
| 读写分离 | 删除 / 数据库优化 | 未确认时 |
| OSS对象存储 | 文件存储 / 上传图片 | 未确认时 |

---

## ✂️ 内容精简规范

### 删除内容类型清单

#### 1. 具体技术名称
- ❌ @Async、@Service、@RestController、@FeignClient
- ✅ 异步注解、服务注解、控制器、声明式客户端

#### 2. 具体类名/工具名
- ❌ BaseMapper、QueryWrapper、BCryptPasswordEncoder、RedisTemplate
- ✅ 基础接口、条件构造器、加密工具、缓存模板

#### 3. 具体参数数值
- ❌ 10、50、30秒、7天、QPS为5000
- ✅ 合理的参数、适当的时间、流量阈值

#### 4. 具体字段名
- ❌ category_id、price、sales、user_id、status
- ✅ 分类字段、价格字段、高频查询字段

#### 5. 具体代码/命令
- ❌ SELECT * WHERE...、HGETALL、SETNX、EXPLAIN
- ✅ 查询语句、获取数据、设置锁、分析执行计划

#### 6. 具体路径/格式
- ❌ POST /api/user/register、cart:userId
- ✅ 注册接口、按用户组织数据

---

## 📐 章节结构调整规范

### 规范1：设计与实现分离

**适用章节**：第4章（设计）、第5章（实现）

**调整规则**：
```
第4章设计章节：
  ✅ 保留：第一段总述
  ✅ 保留：设计图表
  ❌ 删除：第二段及之后的详细实现描述

第5章实现章节：
  ✅ 接收：从第4章删除的实现内容
  ✅ 精简：删除代码级细节
  ✅ 改为：方案级描述
```

**示例**：
```
4.4 微服务治理与基础设施设计：
  第一段：微服务治理是微服务架构的核心环节...如图4-18所示。
  ❌ 删除后续所有段落

5.3 微服务治理与基础设施实现：
  ✅ 整合4.4删除的内容
  ✅ 精简技术细节
```

---

### 规范2：数据库章节三层结构

**适用章节**：4.2 数据库设计

**分层规则**：
```
4.2.1 概念结构设计：
  - 概念模型说明
  - 实体关系概述

4.2.2 逻辑结构设计：
  - ✅ 开头总述（保留）
  - ✅ 每个表的ER图
  - ✅ 每个表一句话描述
  - ❌ 删除详细字段列举

4.2.3 表结构设计：
  - ✅ 详细字段说明（主键、外键、类型）
  - ✅ 三线表引用
  - ❌ 删除索引设计说明
```

---

### 规范3：测试章节极简化

**适用章节**：第6章 系统测试

**精简规则**：

**6.1 测试环境与策略**：
```
✅ 保留：第一段（测试概述）
✅ 保留：第二段（测试环境）
❌ 删除：第三段及之后
精简比例：80%
```

**6.2.x 功能测试子章节**：
```
模板：
  XX功能测试验证[功能列表]的正确性。详细测试用例如表X-Y所示。

✅ 一句话 + 表格引用
❌ 删除所有详细测试步骤
精简比例：95%以上
```

**6.3 性能测试**：
```
✅ 保留：第一段（测试工具和目标）
✅ 保留：第二段（测试环境，删除具体参数）
✅ 保留：最后一段（测试结论，删除具体数据）
❌ 删除：第三段到倒数第二段（所有详细测试数据）
精简比例：90%
```

---

## 🔧 技术准确性修改指南

### 检查点1：对照source-truth.json

**必查内容**：
```json
{
  "implementedFeatures": [
    "用户注册登录认证",  // ✅ 可以写
    "商品分类管理",      // ✅ 可以写
    ...
  ],
  "prohibitedTechnologies": [
    "Kubernetes",        // ❌ 必须删除
    "Elasticsearch",     // ❌ 必须删除
    "Kafka"              // ❌ 必须删除
  ]
}
```

---

### 检查点2：删除禁用技术

**自动检查命令**：
```bash
# 检查章节文件
grep -ri "kubernetes\|k8s\|elasticsearch\|kafka" paper/chapters/

# 检查大纲文件
grep -i "kubernetes\|elasticsearch\|kafka" paper/outline.json
```

**删除策略**：
```
直接删除：
  - "容器化部署使用Docker和Kubernetes"
  - "搜索功能集成Elasticsearch"
  - "消息队列使用Kafka"

泛化描述：
  - Docker/K8S → "容器编排平台"
  - Elasticsearch → 删除或"搜索功能"
  - Prometheus+Grafana → "监控系统"
```

---

### 检查点3：删除未确认技术

**常见未确认技术**：
- 读写分离、主从复制
- 分库分表、水平拆分
- 对象存储（OSS、S3）
- 微服务网格（Istio、Linkerd）
- 复杂算法（如果只是配置化实现）

**处理方式**：
```
❌ 删除："读写分离配置MySQL主从复制"
✅ 保留："数据库连接池配置"

❌ 删除："调用对象存储服务上传图片"
✅ 改为："上传图片获取图片地址"

❌ 删除："热门商品推荐基于多维度指标计算（加权公式：销量×0.5...）"
✅ 改为："轮播图和推荐商品配置"
```

---

### 检查点4：用词准确性

**原则**：避免过度承诺

```
❌ "系统遵循领域驱动设计方法"
✅ "系统参考领域驱动设计方法"

❌ "系统完全实现了微服务治理"
✅ "系统实现了微服务治理机制"

❌ "系统采用了最先进的..."
✅ "系统采用了..."
```

---

## 🎨 图表质量规范

### ER图标准

**布局规则**：
- ✅ 单层圆环布局（不用多层）
- ✅ 根据字段数量动态调整半径
  - ≤8个字段：半径250px
  - 9-12个字段：半径320px
  - 13-16个字段：半径380px
  - ≥17个字段：半径420px
- ✅ 连线精确从矩形边缘到椭圆边缘
- ✅ 字段均匀分布在圆周上

**工具脚本**：
```bash
python3 tools/generate-er-optimized.py paper/assets/tables
```

---

### UML图标准

**基本要求**：
- ✅ 使用PlantUML绘制
- ✅ 基于真实功能和流程
- ✅ 简洁清晰，避免过度复杂
- ❌ 不包含未实现的功能

**渲染命令**：
```bash
plantuml -tpng paper/assets/plantuml/*.puml
```

---

## 📊 修改流程SOP

### 标准修改流程

```
1. 读取 source-truth.json
   ↓
2. 检查禁用技术（全文搜索）
   ↓
3. 检查未确认技术
   ↓
4. 按章节类型调整结构
   - 设计章节：精简为总述+图
   - 实现章节：删除代码级细节
   - 测试章节：精简为概述+表格
   ↓
5. 修改用词（遵循→参考）
   ↓
6. 优化图表质量
   ↓
7. 验证技术准确性
   ↓
8. 导出最终DOCX
```

---

## ✅ 修改完成验证清单

### 技术准确性验证
- [ ] 已对照source-truth.json
- [ ] 已删除所有prohibitedTechnologies
- [ ] 已删除所有未确认技术
- [ ] 所有功能都在implementedFeatures中

### 风格转换验证
- [ ] 已删除所有注解名称（@Xxx）
- [ ] 已删除所有具体参数值
- [ ] 已删除所有SQL代码示例
- [ ] 已删除所有具体字段名列举
- [ ] 已删除所有接口路径

### 结构调整验证
- [ ] 设计章节已精简（只保留总述+图）
- [ ] 实现章节已优化（方案级描述）
- [ ] 测试章节已极简（概述+表格）
- [ ] 数据库章节分层清晰

### 用词准确性验证
- [ ] "遵循"已改为"参考"
- [ ] 无过度技术承诺
- [ ] 无绝对化表述

### 图表质量验证
- [ ] ER图连线精确
- [ ] ER图布局工整
- [ ] UML图基于真实功能
- [ ] 图表编号正确

---

## 📝 快速参考卡

### 记住这5条

1. **真实性第一**：只写source-truth.json中确认的
2. **设计要轻**：总述+图，不写实现细节
3. **实现要精**：方案级，不写代码级
4. **测试要简**：一句话+表格
5. **用词要准**："参考"不"遵循"，避免承诺

---

## 🔄 适用其他项目的步骤

### 步骤1：准备source-truth.json
扫描新项目的技术栈，生成source-truth.json

### 步骤2：执行技术准确性检查
```bash
grep -ri "kubernetes\|elasticsearch\|kafka\|docker" paper/chapters/
```

### 步骤3：应用本规范
按照本文档的规则逐章修改

### 步骤4：验证
使用验证清单检查

### 步骤5：导出
执行DOCX导出流程

---

**文档版本**: 1.0
**最后更新**: 2025-10-11
**适用范围**: 所有基于真实项目的毕业论文
**维护**: 持续更新补充
