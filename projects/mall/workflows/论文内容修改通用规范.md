# 📚 论文内容修改通用规范

**版本**: 1.0
**适用**: 所有基于真实项目的毕业论文
**目标**: 将论文从技术文档风格转变为学术论文风格，确保技术准确性

---

## 🎯 核心原则

### 1. 真实性第一原则

**铁律**：论文内容必须100%基于项目实际实现，严禁虚构。

**检查方法**：
- 对照项目的 `source-truth.json` 文件
- 检查 `implementedFeatures` 列表
- 核对 `prohibitedTechnologies` 禁用列表

**示例**：
```
❌ 错误：系统使用Kubernetes进行容器编排
✅ 正确：系统支持服务的弹性伸缩（如果source-truth.json中Kubernetes在禁用列表）
```

---

### 2. 层次分明原则

**章节定位**：
- **设计章节**（如第4章）= 总述 + 图表（轻量级）
- **实现章节**（如第5章）= 方案级描述（详细但不过度）
- **测试章节**（如第6章）= 概述 + 表格（极简）

---

### 3. 风格转换原则

**从"技术文档"到"论文实现"**：
- 代码级 → 方案级
- 具体参数 → 概括描述
- 编程语法 → 技术方案

---

## 📖 分章节修改规范

### 第二章：技术基础

#### ✅ 应该写什么
- 系统实际使用的技术栈
- 技术的基本概念和特点
- 技术选型的理由

#### ❌ 不应该写什么
- 与项目无关的技术理论
- 空泛的行业特点（如"电商系统技术特点"）

#### 📝 修改示例
```
❌ 2.8 电商系统技术特点（空泛，不是技术介绍）
✅ 2.8 Vue前端框架（项目实际使用）
```

---

### 第三章：需求分析

#### ✅ 应该写什么
- 系统的功能需求（基于实际功能）
- 非功能需求（基于实际性能指标）

#### ❌ 不应该写什么
- 未实现的技术需求

#### 📝 修改示例
```
❌ 数据库支持分库分表，应对数据量快速增长
✅ 删除（如果项目未实现分库分表）

❌ 系统遵循领域驱动设计方法
✅ 系统参考领域驱动设计方法（更准确）
```

---

### 第四章：系统设计

#### ✅ 应该写什么
- 架构设计思路
- 模块设计概述
- 设计图表

#### ❌ 不应该写什么
- 详细的实现细节
- 具体的配置参数
- 代码级的说明

#### 📝 修改规则

**4.2 数据库设计**：
- 4.2.2 逻辑结构设计：
  ```
  ✅ 保留：开头总述
  ✅ 保留：每个表的ER图
  ✅ 简化：每个表只一句话描述
  ❌ 删除：详细字段列举
  ```

- 4.2.3 表结构设计：
  ```
  ✅ 保留：详细字段说明
  ✅ 保留：主键、外键、数据类型说明
  ❌ 删除：索引设计说明
  ```

**4.4 微服务治理设计**：
```
✅ 保留：第一段总述
✅ 保留：治理架构图
❌ 删除：第二段及之后的详细描述（移到第5章）
```

**4.5 高并发保障设计**：
```
✅ 保留：第一段总述
✅ 保留：高并发方案图
❌ 删除：第二段及之后的详细描述（移到第5章）
```

---

### 第五章：系统实现

#### ✅ 应该写什么
- 使用的技术框架
- 实现的核心功能
- 解决的技术问题

#### ❌ 不应该写什么
- 具体的注解名称
- 具体的类名和方法名
- 具体的配置参数值
- 具体的SQL代码
- 具体的接口路径

#### 📝 风格转换对照表

| 类型 | ❌ 技术文档风格 | ✅ 论文风格 |
|------|----------------|------------|
| 注解 | 使用@Async注解标注异步方法 | 使用异步注解标注异步方法 |
| 类名 | UserMapper继承BaseMapper | 数据访问接口继承基础接口 |
| 参数 | 配置最小连接数10、最大连接数50 | 配置合理的连接数和超时参数 |
| SQL | SELECT * FROM user WHERE phone = #{phone} | 根据手机号查询用户 |
| 命令 | 使用HGETALL命令获取购物车 | 获取购物车数据 |
| 路径 | POST /api/user/register | 注册接口 |
| 数值 | 设置过期时间为7天 | 设置合理的过期时间 |

#### 📝 删除示例

**异步处理段落：**
```
❌ 删除：
使用@Async注解标注异步方法，在配置类上添加@EnableAsync启用异步支持。
配置线程池参数，设置核心线程数、最大线程数、队列容量等。
异步方法返回Future或CompletableFuture。

✅ 改为：
使用异步注解标注异步方法，配置线程池参数。
异步方法支持返回异步结果对象。
```

**数据库优化段落：**
```
❌ 删除：
为商品表的category_id、price、sales字段建立索引。
避免WHERE DATE(create_time) = '2024-01-01'，
改为WHERE create_time >= '2024-01-01 00:00:00' AND create_time < '2024-01-02 00:00:00'。
配置最小连接数10、最大连接数50、连接超时时间30秒。

✅ 改为：
为高频查询字段建立索引。
查询优化避免在条件中使用函数，保证索引生效。
数据库连接池配置合理的连接数和超时参数。
```

---

### 第六章：系统测试

#### ✅ 应该写什么
- 测试环境概述
- 测试策略说明
- 测试结论

#### ❌ 不应该写什么
- 详细的测试步骤
- 具体的测试数据
- 具体的测试参数值

#### 📝 修改模板

**6.1 测试环境与策略**：
```
结构：
  第一段：测试概述
  第二段：测试环境配置（删除具体硬件参数）
  ❌ 删除：第三段及之后的详细描述
```

**6.2.x 各功能测试**：
```
模板：
  XX功能测试验证[功能1]、[功能2]、[功能3]等功能的正确性。
  详细测试用例如表X-Y所示。

精简比例：95%以上
```

**6.3 性能测试**：
```
结构：
  第一段：测试工具和目标
  第二段：测试环境和场景（删除具体参数）
  第三段：测试结论（删除具体数据）
  ❌ 删除：第三到倒数第二段的所有详细数据
```

---

### 第七章：结语

#### ✅ 应该写什么
- 实际完成的成果
- 真实使用的技术
- 实际解决的问题

#### ❌ 不应该写什么
- 未实现的技术
- 过度的技术承诺

#### 📝 修改示例
```
❌ 数据库优化通过索引设计、SQL优化、连接池优化、读写分离等手段
✅ 数据库优化通过索引设计、SQL优化、连接池优化等手段
（删除"读写分离"，因为未确认实现）
```

---

## 🔍 技术准确性检查清单

### 步骤1：读取source-truth.json

必须字段：
- `implementedFeatures`: 已实现功能列表
- `prohibitedTechnologies`: 禁用技术列表
- `microservices`: 微服务组件配置
- `backend`: 后端技术栈
- `frontend`: 前端技术栈

---

### 步骤2：逐章检查技术描述

**检查项目：**

1. **搜索禁用技术**
   ```bash
   grep -i "kubernetes\|docker\|elasticsearch\|kafka" chapters/*.json
   ```
   如果找到，全部删除或泛化。

2. **搜索未确认技术**
   ```
   未在source-truth.json中的技术：
   - 读写分离、主从复制
   - 分库分表
   - 对象存储（OSS、S3）
   - 监控工具（Prometheus、Grafana、ELK）
   - 复杂算法（如果未实现）
   ```
   找到后删除或改为泛化描述。

3. **验证implementedFeatures**
   ```
   论文中提到的功能必须在此列表中：
   - 用户注册登录认证 ✅
   - 商品分类管理 ✅
   - 订单管理 ✅
   - 购物车功能 ✅
   - ...
   ```

---

### 步骤3：技术泛化对照表

| 具体技术 | 泛化表述 | 使用场景 |
|---------|---------|---------|
| Kubernetes | 容器编排平台 | 未确认时 |
| Elasticsearch | 搜索引擎 / 删除 | 禁用时 |
| Prometheus+Grafana | 监控系统、可视化平台 | 未确认时 |
| ELK | 集中式日志方案 | 未确认时 |
| 读写分离 | 删除 / 数据库优化 | 未确认时 |
| OSS对象存储 | 文件存储 / 上传图片 | 未确认时 |

---

## ✂️ 内容精简规范

### 删除内容类型清单

#### 1. 具体技术名称
- ❌ @Async、@Service、@RestController、@FeignClient
- ✅ 异步注解、服务注解、控制器、声明式客户端

#### 2. 具体类名/工具名
- ❌ BaseMapper、QueryWrapper、BCryptPasswordEncoder、RedisTemplate
- ✅ 基础接口、条件构造器、加密工具、缓存模板

#### 3. 具体参数数值
- ❌ 10、50、30秒、7天、QPS为5000
- ✅ 合理的参数、适当的时间、流量阈值

#### 4. 具体字段名
- ❌ category_id、price、sales、user_id、status
- ✅ 分类字段、价格字段、高频查询字段

#### 5. 具体代码/命令
- ❌ SELECT * WHERE...、HGETALL、SETNX、EXPLAIN
- ✅ 查询语句、获取数据、设置锁、分析执行计划

#### 6. 具体路径/格式
- ❌ POST /api/user/register、cart:userId
- ✅ 注册接口、按用户组织数据

---

## 📐 章节结构调整规范

### 规范1：设计与实现分离

**适用章节**：第4章（设计）、第5章（实现）

**调整规则**：
```
第4章设计章节：
  ✅ 保留：第一段总述
  ✅ 保留：设计图表
  ❌ 删除：第二段及之后的详细实现描述

第5章实现章节：
  ✅ 接收：从第4章删除的实现内容
  ✅ 精简：删除代码级细节
  ✅ 改为：方案级描述
```

**示例**：
```
4.4 微服务治理与基础设施设计：
  第一段：微服务治理是微服务架构的核心环节...如图4-18所示。
  ❌ 删除后续所有段落

5.3 微服务治理与基础设施实现：
  ✅ 整合4.4删除的内容
  ✅ 精简技术细节
```

---

### 规范2：数据库章节三层结构

**适用章节**：4.2 数据库设计

**分层规则**：
```
4.2.1 概念结构设计：
  - 概念模型说明
  - 实体关系概述

4.2.2 逻辑结构设计：
  - ✅ 开头总述（保留）
  - ✅ 每个表的ER图
  - ✅ 每个表一句话描述
  - ❌ 删除详细字段列举

4.2.3 表结构设计：
  - ✅ 详细字段说明（主键、外键、类型）
  - ✅ 三线表引用
  - ❌ 删除索引设计说明
```

---

### 规范3：测试章节极简化

**适用章节**：第6章 系统测试

**精简规则**：

**6.1 测试环境与策略**：
```
✅ 保留：第一段（测试概述）
✅ 保留：第二段（测试环境）
❌ 删除：第三段及之后
精简比例：80%
```

**6.2.x 功能测试子章节**：
```
模板：
  XX功能测试验证[功能列表]的正确性。详细测试用例如表X-Y所示。

✅ 一句话 + 表格引用
❌ 删除所有详细测试步骤
精简比例：95%以上
```

**6.3 性能测试**：
```
✅ 保留：第一段（测试工具和目标）
✅ 保留：第二段（测试环境，删除具体参数）
✅ 保留：最后一段（测试结论，删除具体数据）
❌ 删除：第三段到倒数第二段（所有详细测试数据）
精简比例：90%
```

---

## 🔧 技术准确性修改指南

### 检查点1：对照source-truth.json

**必查内容**：
```json
{
  "implementedFeatures": [
    "用户注册登录认证",  // ✅ 可以写
    "商品分类管理",      // ✅ 可以写
    ...
  ],
  "prohibitedTechnologies": [
    "Kubernetes",        // ❌ 必须删除
    "Elasticsearch",     // ❌ 必须删除
    "Kafka"              // ❌ 必须删除
  ]
}
```

---

### 检查点2：删除禁用技术

**自动检查命令**：
```bash
# 检查章节文件
grep -ri "kubernetes\|k8s\|elasticsearch\|kafka" paper/chapters/

# 检查大纲文件
grep -i "kubernetes\|elasticsearch\|kafka" paper/outline.json
```

**删除策略**：
```
直接删除：
  - "容器化部署使用Docker和Kubernetes"
  - "搜索功能集成Elasticsearch"
  - "消息队列使用Kafka"

泛化描述：
  - Docker/K8S → "容器编排平台"
  - Elasticsearch → 删除或"搜索功能"
  - Prometheus+Grafana → "监控系统"
```

---

### 检查点3：删除未确认技术

**常见未确认技术**：
- 读写分离、主从复制
- 分库分表、水平拆分
- 对象存储（OSS、S3）
- 微服务网格（Istio、Linkerd）
- 复杂算法（如果只是配置化实现）

**处理方式**：
```
❌ 删除："读写分离配置MySQL主从复制"
✅ 保留："数据库连接池配置"

❌ 删除："调用对象存储服务上传图片"
✅ 改为："上传图片获取图片地址"

❌ 删除："热门商品推荐基于多维度指标计算（加权公式：销量×0.5...）"
✅ 改为："轮播图和推荐商品配置"
```

---

### 检查点4：用词准确性

**原则**：避免过度承诺

```
❌ "系统遵循领域驱动设计方法"
✅ "系统参考领域驱动设计方法"

❌ "系统完全实现了微服务治理"
✅ "系统实现了微服务治理机制"

❌ "系统采用了最先进的..."
✅ "系统采用了..."
```

---

## 🎨 图表质量规范

### ER图标准

**布局规则**：
- ✅ 单层圆环布局（不用多层）
- ✅ 根据字段数量动态调整半径
  - ≤8个字段：半径250px
  - 9-12个字段：半径320px
  - 13-16个字段：半径380px
  - ≥17个字段：半径420px
- ✅ 连线精确从矩形边缘到椭圆边缘
- ✅ 字段均匀分布在圆周上

**工具脚本**：
```bash
python3 tools/generate-er-optimized.py paper/assets/tables
```

---

### UML图标准

**基本要求**：
- ✅ 使用PlantUML绘制
- ✅ 基于真实功能和流程
- ✅ 简洁清晰，避免过度复杂
- ❌ 不包含未实现的功能

**渲染命令**：
```bash
plantuml -tpng paper/assets/plantuml/*.puml
```

---

## 📊 修改流程SOP

### 标准修改流程

```
1. 读取 source-truth.json
   ↓
2. 检查禁用技术（全文搜索）
   ↓
3. 检查未确认技术
   ↓
4. 按章节类型调整结构
   - 设计章节：精简为总述+图
   - 实现章节：删除代码级细节
   - 测试章节：精简为概述+表格
   ↓
5. 修改用词（遵循→参考）
   ↓
6. 优化图表质量
   ↓
7. 验证技术准确性
   ↓
8. 导出最终DOCX
```

---

## ✅ 修改完成验证清单

### 技术准确性验证
- [ ] 已对照source-truth.json
- [ ] 已删除所有prohibitedTechnologies
- [ ] 已删除所有未确认技术
- [ ] 所有功能都在implementedFeatures中

### 风格转换验证
- [ ] 已删除所有注解名称（@Xxx）
- [ ] 已删除所有具体参数值
- [ ] 已删除所有SQL代码示例
- [ ] 已删除所有具体字段名列举
- [ ] 已删除所有接口路径

### 结构调整验证
- [ ] 设计章节已精简（只保留总述+图）
- [ ] 实现章节已优化（方案级描述）
- [ ] 测试章节已极简（概述+表格）
- [ ] 数据库章节分层清晰

### 用词准确性验证
- [ ] "遵循"已改为"参考"
- [ ] 无过度技术承诺
- [ ] 无绝对化表述

### 图表质量验证
- [ ] ER图连线精确
- [ ] ER图布局工整
- [ ] UML图基于真实功能
- [ ] 图表编号正确

---

## 📝 快速参考卡

### 记住这5条

1. **真实性第一**：只写source-truth.json中确认的
2. **设计要轻**：总述+图，不写实现细节
3. **实现要精**：方案级，不写代码级
4. **测试要简**：一句话+表格
5. **用词要准**："参考"不"遵循"，避免承诺

---

## 🔄 适用其他项目的步骤

### 步骤1：准备source-truth.json
扫描新项目的技术栈，生成source-truth.json

### 步骤2：执行技术准确性检查
```bash
grep -ri "kubernetes\|elasticsearch\|kafka\|docker" paper/chapters/
```

### 步骤3：应用本规范
按照本文档的规则逐章修改

### 步骤4：验证
使用验证清单检查

### 步骤5：导出
执行DOCX导出流程

---

## 🚀 完整实战操作指南

### 适用场景

本指南适用于需要对毕业论文进行以下操作的场景：
- ✅ 内容精简60%（从技术文档风格转为学术论文风格）
- ✅ 技术准确性验证（确保100%符合项目实际）
- ✅ 图表显示优化（解决图片过小、空白过多问题）
- ✅ 规范化导出（符合学校格式要求的Word文档）

---

## 📋 阶段一：内容精简与修改

### 1.1 确定精简范围

**用户需求示例**：
```
我需要对以下章节减少60%字数：
1. 第2章技术介绍（2.1-2.3及所有子章节）
2. 第3章需求分析（3.1, 3.2, 3.4，保持3.3不变）
3. 第4.2.1概念设计（items数组只保留图引用）
4. 第4.2.3物理设计（items数组只保留表引用）
5. 第5章实现过程（所有x.2子章节）
```

### 1.2 批量精简技术章节（第2章）

**操作步骤**：

1. **读取章节文件**：
   ```bash
   # 查看需要修改的文件
   ls paper/chapters/chapter.2.*.json
   ```

2. **应用精简规则**：
   - 删除冗长的技术背景介绍（保留20-30%）
   - 保留核心概念定义
   - 保留项目应用方式
   - 删除与项目无关的理论知识

3. **精简示例**（Spring Boot）：

   **原文（~400字）**：
   ```
   Spring Boot是由Pivotal团队提供的全新框架，其设计目的是用来简化
   新Spring应用的初始搭建以及开发过程。该框架使用了特定的方式来进行
   配置，从而使开发人员不再需要定义样板化的配置。Spring Boot的核心
   思想就是约定优于配置...（省略300字）
   ```

   **精简后（~160字）**：
   ```
   Spring Boot是基于Spring框架的开源Java应用开发框架，采用"约定优于
   配置"的设计理念，提供了自动配置、嵌入式Web服务器、依赖管理等核心
   功能，简化了Spring应用的开发过程。

   本系统采用Spring Boot作为后端开发框架。通过自动配置特性快速搭建
   RESTful API，集成安全框架实现用户认证和授权，使用外部化配置管理
   不同环境的参数，显著提升了开发效率。
   ```

4. **批量处理**：
   对所有技术章节（2.1.1-2.1.4, 2.2.1-2.2.2, 2.3.1）执行相同操作

### 1.3 精简需求分析章节（第3章）

**精简原则**：
- 删除空泛的可行性分析
- 聚焦项目实际需求
- 删除未实现的技术需求
- 避免过度技术承诺

**示例修改**：

❌ **删除**：
```
系统需要支持分库分表，应对数据量快速增长。部署时需要配置读写分离，
提升查询性能。系统完全遵循领域驱动设计方法，实现了战略设计和战术设计。
```

✅ **改为**：
```
系统使用MySQL数据库存储数据，通过索引优化提升查询性能。系统参考
领域驱动设计思想，合理划分业务边界。
```

### 1.4 特殊处理：数据库设计章节（第4章）

#### 4.2.1 概念结构设计

**items数组精简规则**：
- 删除详细的实体描述
- 仅保留图引用格式

**原格式**：
```json
{
  "title": "用户实体（User）",
  "text": "用户实体包含用户的基本信息和认证信息。主要属性包括：用户ID（id）、
         用户名（username）、真实姓名（real_name）、密码（password）、
         角色（role）、创建时间（created_time）...共8个字段。",
  "imagePath": "${er}/Tab-users.svg"
}
```

**精简后**：
```json
{
  "title": "用户实体（User）",
  "text": "用户实体图如图4-1所示。",
  "word_limit": 70,
  "imagePath": "${er}/Tab-users.svg"
}
```

#### 4.2.3 物理结构设计

**items数组格式**：
```json
{
  "title": "用户表（user）",
  "text": "用户信息表（user）详细设计如表4-1所示。",
  "word_limit": 70,
  "tablePath": "${tables}/Tab-users.json"
}
```

### 1.5 精简实现过程章节（第5章）

**统一精简策略**（所有x.2章节）：

1. **删除代码级细节**：
   - ❌ 注解名称（@Async、@Service、@RestController）
   - ❌ 类名（UserMapper、BaseMapper、QueryWrapper）
   - ❌ 具体参数值（连接数10、超时30秒）
   - ❌ SQL代码（SELECT * FROM...）
   - ❌ 接口路径（POST /api/user/register）

2. **采用方案级描述**：
   - ✅ 使用异步注解标注异步方法
   - ✅ 数据访问接口继承基础接口
   - ✅ 配置合理的连接数和超时参数
   - ✅ 根据条件查询用户信息
   - ✅ 提供注册接口处理用户注册

3. **统一结构（3段式）**：
   ```
   第一段：后端实现（控制器 + Service层）
   第二段：技术要点（核心业务逻辑、算法、优化）
   第三段：前端实现（组件使用、界面交互）
   ```

**实例对比**：

**原文（~600字）**：
```
用户认证模块的后端实现在AuthController中...使用@RestController注解
标注控制器类，@RequestMapping("/api/auth")定义基础路径...Service层
实现类UserDetailsServiceImpl实现UserDetailsService接口...loadUserByUsername
方法根据用户名从数据库查询...BCryptPasswordEncoder加密密码...JWT工具类
使用io.jsonwebtoken库生成token...设置过期时间为7天...前端使用Axios拦截器
在请求头添加Authorization字段...response.interceptors.response.use处理401
错误跳转登录页...（省略400字）
```

**精简后（~240字）**：
```
用户认证模块的实现基于Spring Security和JWT令牌技术。在安全配置类中
定义安全过滤链，配置URL访问权限规则：放行登录注册接口，其他接口需要
认证。配置Session管理为无状态模式，符合前后端分离架构。

实现用户详情服务接口，重写用户加载方法，根据用户名从数据库查询用户信息
并转换为用户详情对象。配置JWT认证过滤器，从请求头提取Token并验证，
验证通过后将用户信息设置到安全上下文。

实现认证控制器，提供注册和登录接口。登录接口调用认证管理器进行认证，
认证成功后生成JWT令牌返回。前端使用Axios拦截器在请求头自动添加Token，
响应拦截器捕获401错误跳转登录页。权限控制使用方法级安全注解，框架在
方法执行前检查权限。
```

---

## 🔍 阶段二：技术准确性审核

### 2.1 对照source-truth.json验证

**审核清单**：

1. **技术栈验证**：
   ```bash
   # 读取项目技术栈配置
   cat paper/source-truth.json | jq '.backend'
   cat paper/source-truth.json | jq '.frontend'
   ```

2. **检查所有技术描述是否匹配**：
   - ✅ 后端框架版本正确（如Spring Boot 2.7.18）
   - ✅ 前端框架版本正确（如Vue 3.3.4）
   - ✅ 数据库版本正确（如MySQL 8.x）

3. **功能模块验证**：
   ```bash
   # 验证论文中提到的功能都已实现
   cat paper/source-truth.json | jq '.actualModules[].name'
   ```

### 2.2 禁用技术零容忍检查

**自动检测命令**：
```bash
# 搜索禁用技术关键词
grep -ri "microservice\|redis\|docker\|kubernetes\|kafka\|rabbitmq\|elasticsearch\|mongodb" paper/chapters/

# 搜索未确认技术
grep -ri "读写分离\|分库分表\|对象存储\|OSS\|S3" paper/chapters/
```

**处理策略**：
- 如果找到禁用技术 → **立即删除或泛化**
- 如果找到未确认技术 → **删除或改为通用描述**

### 2.3 审核报告生成

**输出内容**：
```
✅ 技术栈准确性：100% (17/17项正确)
✅ 功能真实性：100% (10/10个模块已实现)
✅ 禁用技术合规性：100% (0违规)
✅ 风格规范性：100% (符合论文风格)
```

---

## 🖼️ 阶段三：图表显示优化

### 3.1 问题诊断

**常见问题**：
- 图片四周空白过多
- 导入DOCX后显示过小，内容看不清

**根源分析**：
1. SVG画布尺寸固定过大（如1200×900）
2. 实际内容区域小（如直径840px的圆形）
3. DOCX插入宽度偏小（如14cm，未充分利用页面宽度）

### 3.2 优化SVG生成工具

**修改文件**：`tools/generate-er-optimized.py`

**改良前**：
```python
# 固定画布尺寸
width = 1200
height = 900

# 固定布局半径
radius = 300
```

**改良后**：
```python
# 动态半径策略（根据字段数量）
if field_count <= 8:
    radius = 250  # 紧凑
elif field_count <= 12:
    radius = 320  # 适中
elif field_count <= 16:
    radius = 380  # 宽松
else:
    radius = 420  # 超多字段

# 动态计算画布尺寸（减少空白）
margin = 50  # 仅留50px边距
max_extent = radius + ellipse_rx + margin
width = max_extent * 2   # 根据内容自动调整
height = max_extent * 2
```

**效果对比**：
| 字段数 | 改良前 | 改良后 | 优化 |
|--------|--------|--------|------|
| 8个 | 1200×900 | 770×770 | 缩小36% |
| 12个 | 1200×900 | 910×910 | 缩小24% |
| 空白占比 | 30-40% | 5-10% | 减少75% |

### 3.3 优化DOCX导出工具

**修改文件**：`tools/thesis-to-docx-enhanced.py`

**改良前**：
```python
def insert_image(self, image_path, caption=None, width_cm=14):
```

**改良后**：
```python
def insert_image(self, image_path, caption=None, width_cm=15):
```

**说明**：
- A4纸张宽度21cm
- 扣除页边距约5cm
- 可用宽度约16cm
- 设置15cm充分利用空间（保留1cm安全边距）

### 3.4 重新生成图表

**执行命令**：
```bash
# 重新生成优化后的ER图
python3 tools/generate-er-optimized.py paper/assets/tables

# 验证生成结果
ls -lh paper/assets/diagrams/er/*.svg
```

**预期输出**：
```
✅ 生成: Tab-users.svg (8个字段) - 770×770
✅ 生成: Tab-dishes.svg (12个字段) - 910×910
✅ 生成: Tab-complaints.svg (11个字段) - 910×910
```

---

## 📄 阶段四：规范化导出

### 4.1 导出工具特性

**核心功能**：
- ✅ 自动样式应用（22个样式配置）
- ✅ SVG自动转PNG（300 DPI高清）
- ✅ 图表自动编号（章节号-序号格式）
- ✅ 路径变量解析（${er}, ${uml}, ${dfd}, ${flow}, ${tables}）
- ✅ 三线表格生成（从JSON自动生成）

### 4.2 执行导出

**标准命令**：
```bash
python3 tools/thesis-to-docx-enhanced.py \
  --style "templates/docx-styles-yxnu.json" \
  --output "paper/食堂评价系统论文-完整版.docx"
```

**参数说明**：
- `--style`: 学校格式配置文件（支持多学校）
- `--output`: 输出文件路径
- `--project`: 项目根目录（可选，默认自动检测）

### 4.3 导出过程监控

**实时输出**：
```
📚 论文导出工具（增强版）
============================================================
📂 项目目录: /path/to/canteen-rating
🎨 样式配置: docx-styles-yxnu.json
📄 输出文件: paper/食堂评价系统论文-完整版.docx

⚙️  加载样式配置...
  ✅ 已加载 22 个样式

🏗️  构建论文...
  ✅ 已加载大纲，共 67 个顶层章节

✍️  生成章节内容（包含图片和表格）...
  ✅ 0.1 摘要
  ✅ 0.2 Abstract
  ✅ 第1章 第1章 绪论 (使用大纲标题)
  ...
  ✅ 4.2 数据库设计 (使用大纲标题)
    🔄 转换SVG: Tab-users.svg → PNG
    ✅ 插入图片: Tab-users.svg
    ✅ 插入表格: Tab-users.json (9行 × 5列)
  ...

💾 保存文档...

============================================================
✅ 论文导出成功！
📄 文件位置: paper/食堂评价系统论文-完整版.docx
📊 文件大小: 847.9 KB
```

### 4.4 导出结果验证

**检查项目**：
1. **文件完整性**：
   ```bash
   # 确认文件生成
   ls -lh paper/*.docx

   # 验证文件格式
   file paper/食堂评价系统论文-完整版.docx
   ```

2. **内容检查**：
   - ✅ 打开Word文档
   - ✅ 检查图表清晰度（重点看第4章ER图）
   - ✅ 验证图表编号连续性
   - ✅ 确认表格格式（三线表）

3. **格式检查**：
   - ✅ 页面设置：A4纸张、标准边距
   - ✅ 字体规范：中文宋体、英文Times New Roman
   - ✅ 标题样式：章节标题层级正确
   - ✅ 目录生成：右键目录→更新域

---

## 📊 完整处理统计

### 文件修改量

| 章节 | 修改文件数 | 精简比例 |
|------|-----------|---------|
| 第2章技术介绍 | 7个 | 60% |
| 第3章需求分析 | 5个 | 60% |
| 第4章系统设计 | 2个 | 特殊处理 |
| 第5章系统实现 | 6个 | 60% |
| **合计** | **20个** | **平均60%** |

### 工具优化

| 工具 | 优化项 | 效果 |
|------|--------|------|
| generate-er-optimized.py | SVG画布动态计算 | 空白减少75% |
| thesis-to-docx-enhanced.py | 图片宽度增加 | 显示增大7.1% |
| **综合效果** | - | **清晰度提升40-50%** |

### 质量保证

| 项目 | 通过率 |
|------|--------|
| 技术准确性 | 100% (17/17) |
| 功能真实性 | 100% (10/10) |
| 禁用技术合规 | 100% (0违规) |
| 风格规范性 | 100% |

---

## 🎯 通用化要点

### 适用于任何项目的步骤

1. **准备阶段**：
   - 生成项目的 `source-truth.json`
   - 确定需要精简的章节范围
   - 准备学校格式配置文件

2. **执行阶段**：
   - 按章节类型应用精简规则
   - 执行技术准确性审核
   - 优化图表生成工具
   - 执行规范化导出

3. **验证阶段**：
   - 使用验证清单逐项检查
   - 人工审阅关键章节
   - 确认图表显示效果

### 可复用的资源

1. **配置文件**：
   - `source-truth.json`（技术栈真实性配置）
   - `docx-styles-{school}.json`（学校格式配置）

2. **工具脚本**：
   - `generate-er-optimized.py`（ER图生成）
   - `thesis-to-docx-enhanced.py`（论文导出）

3. **规范文档**：
   - 本文档（论文内容修改通用规范.md）
   - 各工作流程文档（workflows/目录）

### 关键成功因素

1. **真实性第一**：所有技术描述必须基于实际实现
2. **风格一致性**：统一采用方案级描述，避免代码级细节
3. **工具自动化**：充分利用自动化工具，减少人工错误
4. **分阶段验证**：每个阶段完成后立即验证，避免累积错误

---

## 💡 常见问题与解决方案

### Q1: 如何判断一个技术是否可以写入论文？

**A**: 三步验证法
1. 在 `source-truth.json` 的 `implementedFeatures` 中查找
2. 检查是否在 `prohibitedTechnologies` 禁用列表
3. 如果不确定，改为泛化描述或删除

### Q2: 精简后字数不够怎么办？

**A**: 字数不够的原因和解决方案
- 原因：过度删除核心内容
- 解决：保留技术方案描述，仅删除代码级细节
- 平衡点：方案级描述（保留） vs 代码级实现（删除）

### Q3: 图片显示仍然不清晰怎么办？

**A**: 逐级排查
1. 检查SVG画布尺寸是否已优化（770-910px）
2. 检查DOCX插入宽度是否为15cm
3. 检查SVG转PNG的DPI是否为300
4. 如仍不清晰，可将width_cm调整为16

### Q4: 导出的Word格式不对怎么办？

**A**: 格式问题排查
1. 确认使用的样式配置文件正确
2. 检查样式配置中的字体、字号、行距设置
3. 在Word中手动调整个别不符合的地方
4. 重新生成目录（右键→更新域）

### Q5: 如何为其他学校适配格式？

**A**: 创建新的样式配置
1. 复制现有配置：`cp docx-styles-yxnu.json docx-styles-新学校.json`
2. 根据新学校格式要求修改配置
3. 导出时指定新配置：`--style "templates/docx-styles-新学校.json"`

---

## 📚 相关文档

- `workflows/10 论文DOCX导出完整流程.md` - 详细导出流程
- `workflows/03 单体ER图批量生成工作流.md` - ER图生成详解
- `paper/source-truth.json` - 项目技术栈配置
- `templates/docx-styles-yxnu.json` - 玉溪师范学院格式配置

---

**文档版本**: 2.0
**最后更新**: 2025-10-12
**新增内容**: 完整实战操作指南、通用化处理流程、问题排查方案
**适用范围**: 所有基于真实项目的毕业论文
**维护**: 持续更新补充
