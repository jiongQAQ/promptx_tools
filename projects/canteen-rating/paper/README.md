# 统一资源目录结构说明

## 📁 目录结构

```
paper/
├── assets/                 # 所有资源统一管理目录
│   ├── tables/            # 所有表格JSON文件（三线表）
│   ├── diagrams/          # 所有图表文件
│   │   ├── er/           # ER图（SVG格式）
│   │   ├── uml/          # UML图（PNG/SVG格式）
│   │   └── impl/         # 实现截图（PNG格式）
│   └── plantuml/         # PlantUML源码文件
├── chapter/              # 章节JSON文件
└── exports/              # 最终导出文件（如thesis.docx）
```

## 🎯 设计原则

### 1. 统一性
- 所有资源文件集中在 `assets/` 目录下
- 按资源类型进行清晰分类
- 避免路径混乱和重复

### 2. 可维护性
- 目录结构清晰，易于管理
- 支持批量操作和自动化处理
- 便于版本控制和备份

### 3. 扩展性
- 支持添加新的资源类型
- 便于集成不同的工具链
- 适应项目发展需求

## 📋 路径规范

### 表格路径
```
"tablePath": "<projectRoot>/paper/assets/tables/{slug}.json"
```

### 图片路径
```
"imagePath": "<projectRoot>/paper/assets/diagrams/uml/{slug}.png"
"imagePath": "<projectRoot>/paper/assets/diagrams/er/{slug}.svg"
```

### PlantUML源码
```
"plantumlPath": "<projectRoot>/paper/assets/plantuml/{slug}.puml"
```

## 🔧 工具适配

### Workflow调整
- **00步骤**: 输出到 `assets/tables/`
- **01步骤**: 输出到 `assets/diagrams/er/`
- **07步骤**: 输出到 `assets/diagrams/uml/`

### 文件命名规范
- 使用 slug 格式：小写、连字符分隔
- 避免特殊字符和空格
- 保持简洁和可读性

## ✅ 优势

1. **路径一致性**: 所有引用路径统一，避免混乱
2. **管理便利性**: 资源集中管理，便于维护
3. **自动化友好**: 支持批量处理和脚本操作
4. **工具兼容性**: 适配所有workflow步骤