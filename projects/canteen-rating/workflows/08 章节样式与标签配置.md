# 📌 章节样式与标签配置工作流

## 🎯 工作流目标
为 `paper/chapters/` 目录下的所有章节JSON文件自动添加：
1. **docx_style 配置**：使用 `08-docx-style-enhancer` 工具为所有章节添加标准化样式配置
2. **图片标签**：为包含 `imagePath` 的章节智能生成 `imageLabel`
3. **表格标签**：为包含 `tablePath` 的章节智能生成 `tableLabel`

## 📊 当前数据状况
- **总章节数量**：54个章节文件
- **目录位置**：`/Users/pc/Documents/promptx_tools/projects/canteen-rating/paper/chapters/`
- **文件模式**：`chapter.*.json` (如 chapter.3.2.json、chapter.6.3.1.json)

## 🔧 第一步：使用工具添加docx_style配置

### 1.1 调用08-docx-style-enhancer工具
```bash
# 工具调用命令
08-docx-style-enhancer工具参数:
{
  "projectPath": "/Users/pc/Documents/promptx_tools/projects/canteen-rating",
  "preset": "academic",
  "dryRun": false,
  "backup": false
}
```

### 1.2 工具执行效果
为所有章节文件添加标准化的docx_style配置：
```json
{
  "docx_style": {
    "preset": "academic"
  }
}
```

## 🏷️ 第二步：智能生成图片和表格标签

### 2.1 图片标签生成规则

**目标章节**：包含 `imagePath` 但缺少 `imageLabel` 的章节

**标签格式**：`图{章节号}-{序号} {描述}`

**编号逻辑**：
- 按章节主号分组计数（如第3章的所有图片）
- 章节内按顺序递增编号
- 示例：`图3-1 系统软件架构图`、`图3-2 数据库设计图`

**智能描述生成**：
根据imagePath文件名和章节内容智能推断：
```javascript
文件名关键词映射：
- 'software-architecture' → '系统软件架构图'
- 'database-design' → '数据库设计图'
- 'use-case' → '用例图'
- 'sequence' → '时序图'
- 'class-diagram' → '类图'
- 'er-diagram' → 'ER图'
- 'system-flow' → '系统流程图'
- 'data-flow' → '数据流程图'
- 默认 → '示意图'
```

### 2.2 表格标签生成规则

**目标章节**：包含 `tablePath` 但缺少 `tableLabel` 的章节

**标签格式**：`表{章节号}-{序号} {描述}`

**编号逻辑**：同图片编号规则
- 示例：`表6-1 用户认证测试用例表`、`表6-2 性能对比表`

**智能描述生成**：
根据tablePath文件名和章节内容智能推断：
```javascript
文件名关键词映射：
- 'test-user-auth' → '用户认证测试用例表'
- 'test-cases' → '测试用例表'
- 'requirements' → '需求分析表'
- 'comparison' → '对比分析表'
- 'performance' → '性能测试表'
- 'api-list' → 'API接口表'
- 'function-test' → '功能测试表'
- 默认 → '数据表'
```

## 📋 执行步骤

### Phase 1: 样式配置（使用工具）
1. **调用08-docx-style-enhancer工具**
   - 工具会自动扫描paper/chapters/目录
   - 为所有缺少docx_style的章节添加配置
   - 使用academic预设样式
   - 无需备份，直接修改原文件

2. **验证工具执行结果**
   - 确认所有54个章节都添加了docx_style配置
   - 检查配置格式正确性

### Phase 2: 智能标签生成（AI分析）
1. **扫描imagePath章节**
   - 遍历所有章节文件
   - 识别包含imagePath但无imageLabel的章节
   - 记录章节信息和图片路径

2. **扫描tablePath章节**
   - 遍历所有章节文件
   - 识别包含tablePath但无tableLabel的章节
   - 记录章节信息和表格路径

3. **AI智能分析生成标签**
   - 分析章节内容和路径信息
   - 按章节号分组生成连续编号
   - 生成准确的功能描述
   - 更新对应章节文件

### Phase 3: 结果验证与统计
- 统计处理的章节数量
- 按章节汇总图表数量
- 验证标签格式和编号的正确性

## 🎯 处理示例

### 图片标签示例
```json
// chapter.3.2.json - 处理前
{
  "id": "3.2",
  "title": "系统软件逻辑架构",
  "imagePath": "/Users/jiongjiong/Documents/a_git_project/promptx_tools/projects/canteen-rating/paper/pngs/software-architecture.png",
  "content": "..."
}

// chapter.3.2.json - 处理后
{
  "id": "3.2",
  "title": "系统软件逻辑架构",
  "imagePath": "/Users/jiongjiong/Documents/a_git_project/promptx_tools/projects/canteen-rating/paper/pngs/software-architecture.png",
  "imageLabel": "图3-1 系统软件架构图",
  "content": "...",
  "docx_style": {
    "preset": "academic"
  }
}
```

### 表格标签示例
```json
// chapter.6.3.1.json - 处理前
{
  "id": "6.3.1",
  "title": "用户认证管理功能测试",
  "tablePath": "/Users/jiongjiong/Documents/a_git_project/promptx_tools/projects/canteen-rating/paper/tables/test-user-auth.json",
  "content": "..."
}

// chapter.6.3.1.json - 处理后
{
  "id": "6.3.1",
  "title": "用户认证管理功能测试",
  "tablePath": "/Users/jiongjiong/Documents/a_git_project/promptx_tools/projects/canteen-rating/paper/tables/test-user-auth.json",
  "tableLabel": "表6-1 用户认证测试用例表",
  "content": "...",
  "docx_style": {
    "preset": "academic"
  }
}
```

## ⚙️ 技术实现要点

### 非侵入性处理
- 只添加缺少的字段，不修改现有内容
- 保持JSON的原有格式和字段顺序
- 每个文件的修改要么全部成功，要么保持原状

### 编号管理策略
- 按章节主号维护图表计数器
- 确保同章节内编号连续递增
- 处理多层嵌套章节号（如6.3.1、6.3.2）

### 错误处理机制
- 文件读取失败：跳过并记录错误
- JSON格式错误：跳过并记录错误
- 单个文件失败不影响其他文件处理

## 🔍 质量验证标准

### 标签规范性
- 格式严格遵循"图X-Y 描述"和"表X-Y 描述"
- 章节内编号连续，无重复无遗漏
- 描述内容与实际图表功能相符

### 样式配置正确性
- 所有章节正确引用"academic"预设
- docx_style配置格式标准化
- 不影响已有的样式配置

### 文件完整性
- 所有修改后的文件都是有效JSON格式
- 除添加字段外，其他内容保持不变
- 处理过程中无数据丢失

## 📊 预期执行结果

```
🚀 工作流08执行完成！

📊 第一步：08-docx-style-enhancer工具处理
- 总章节数: 54
- 添加docx_style配置: 54个章节
- 处理时间: < 10秒

📊 第二步：智能标签生成
- AI分析图片标签: X个章节
  - 第3章: Y个图 (图3-1 到 图3-Y)
  - 第4章: Z个图 (图4-1 到 图4-Z)
  - ...
- AI分析表格标签: M个章节
  - 第6章: N个表 (表6-1 到 表6-N)
  - ...

✅ 所有章节已完成样式配置和标签生成
```

## 🚀 后续工作流集成
本工作流的输出为后续工作流提供标准化输入：
- **09 Word文档生成**：基于配置的样式和标签生成规范Word文档
- **10 图表批量导出**：根据标签信息进行图表批量处理
- **11 引用检查**：验证正文中的图表引用与标签一致性