# 📌 参考论文结构解析

## 🎯 目标

从参考论文 Markdown 文件中提取结构模式、图表分布规律和写作风格特征，为后续工作流提供高优先级的规则依据。

**通用性设计**: 本工作流与具体项目模板无关，可用于任何毕业论文生成项目。

## 📋 适用场景

当 `<projectRoot>/reference-papers/` 目录存在且包含 `.md` 文件时，可执行本工作流：
- 解析参考论文的章节层级结构
- 提取图表使用模式和命名规范
- 识别特殊的结构规则（如父章节是否包含内容）
- 分析字数分配模式
- 学习术语和写作风格特征

## 路径约定

```
<projectRoot>/
├── reference-papers/              # 标准化目录名
│   ├── 参考论文1.md
│   ├── 参考论文2.md
│   └── ...
└── paper/
    ├── reference-structure.json   # 本工作流输出
    └── outline.json
```

## 🔄 执行流程

### Phase 1: 扫描参考论文目录

```bash
# 扫描 reference-papers 目录
Glob({
  "pattern": "*.md",
  "path": "<projectRoot>/reference-papers"
})
```

**输出**:
- 找到的 `.md` 文件列表
- 若有多个文件，提示用户选择（或默认解析第一个）

### Phase 2: 读取参考论文

```bash
# 读取选定的参考论文
Read({
  "file_path": "<projectRoot>/reference-papers/<selected>.md"
})
```

### Phase 3: 结构解析

#### 3.1 章节层级提取

**解析规则**:
```
# 第X章 标题           → level=1, id="X"
## X.Y 标题            → level=2, id="X.Y"
### X.Y.Z 标题         → level=3, id="X.Y.Z"
#### X.Y.Z.W 标题      → level=4, id="X.Y.Z.W"
```

**特殊处理**:
- 摘要、Abstract → id="0.1", id="0.2"
- 致谢 → id="9"
- 参考文献 → id="10"

**提取信息**:
```json
{
  "id": "4.3",
  "title": "系统访问流程",
  "level": 2,
  "parent": "4",
  "has_children": true,
  "has_content": true,
  "word_count": 187
}
```

#### 3.2 图表分布分析

**图片引用识别**:
```markdown
![图4-11](path/to/image.png)
![图X-Y 说明文字](path/to/image.png)
```

**图表类型推断规则**:
| 章节标题关键词 | 推断图表类型 | 路径变量 |
|---------------|-------------|----------|
| "数据流图"/"DFD" | Data Flow Diagram | `${dfd}/dfd-*.png` |
| "流程"/"访问流程" | Flow Diagram | `${dfd}/*-flow.png` |
| "用例"/"use case" | Use Case Diagram | `${uml}/usecase-*.png` |
| "活动图"/"activity" | Activity Diagram | `${uml}/activity-*.png` |
| "架构"/"architecture" | Architecture Diagram | `${uml}/software-architecture.png` |
| "ER图"/"实体关系" | ER Diagram | `${er}/Tab-*.svg` |
| "顺序图"/"时序图" | Sequence Diagram | `${uml}/sequence-*.png` |
| "实现过程"/"截图" | Implementation Screenshot | `${impl}/impl-*.png` |
| "功能描述"/"操作流程" | Flowchart | `${flow}/flow-*.png` |

**提取信息**:
```json
{
  "chapter_id": "4.3",
  "image_ref": "图4-11",
  "image_type": "dfd",
  "image_name": "access-flow",
  "path_variable": "${dfd}/access-flow.png"
}
```

#### 3.3 表格分布分析

**表格引用识别**:
```markdown
表X-Y: 标题
| 列1 | 列2 |
```

**提取信息**:
```json
{
  "chapter_id": "3.2.1",
  "table_ref": "表3-1",
  "table_name": "users",
  "path_variable": "${tables}/Tab-users.json"
}
```

### Phase 4: 特殊规则识别

#### 4.1 父章节内容规则

**检测逻辑**:
```
IF chapter.has_children AND chapter.has_content:
    RECORD as special_parent_pattern
```

**示例**:
```json
{
  "pattern_type": "parent_with_content",
  "examples": [
    {
      "id": "4.3",
      "title": "系统访问流程",
      "has_children": true,
      "has_content": true,
      "has_image": true,
      "keyword_indicators": ["访问", "流程"]
    }
  ]
}
```

#### 4.2 章节命名模式识别

**功能实现章节模式**:
```
第5章 系统实现
├── 5.1 功能A
│   ├── 5.1.1 功能描述    (flowchart)
│   └── 5.1.2 实现过程    (screenshot)
├── 5.2 功能B
│   ├── 5.2.1 功能描述
│   └── 5.2.2 实现过程
```

**提取模式**:
```json
{
  "pattern_type": "implementation_structure",
  "pattern": "{X}.{Y}.1 功能描述 + {X}.{Y}.2 实现过程",
  "description_suffix": "功能描述",
  "implementation_suffix": "实现过程",
  "description_has_flowchart": true,
  "implementation_has_screenshot": true
}
```

**需求分析章节模式**:
```
第2章 需求分析
├── 2.2.1 角色A用例分析
├── 2.2.2 角色B用例分析
```

**提取模式**:
```json
{
  "pattern_type": "usecase_structure",
  "pattern": "{X}.{Y}.{Z} <角色>用例分析",
  "suffix": "用例分析",
  "has_usecase_diagram": true
}
```

#### 4.3 数据流图分层模式

**顶层+二层图模式**:
```
4.3.1 系统数据流图
├── 顶层图 (dfd-top-level.png)
├── 4.3.1.1 功能A二层图 (dfd-A-level2.png)
├── 4.3.1.2 功能B二层图 (dfd-B-level2.png)
```

**提取模式**:
```json
{
  "pattern_type": "dfd_hierarchy",
  "top_level_chapter": "4.3.1",
  "has_top_level_diagram": true,
  "decomposition_chapters": ["4.3.1.1", "4.3.1.2"],
  "naming_pattern": "dfd-{module}-level2.png"
}
```

### Phase 5: 字数分配分析

#### 5.1 按层级统计

统计每个层级章节的平均字数和范围:

```json
{
  "by_level": {
    "level_1": { "avg": 850, "range": [600, 1000], "samples": 6 },
    "level_2": { "avg": 680, "range": [400, 900], "samples": 18 },
    "level_3": { "avg": 520, "range": [300, 700], "samples": 32 },
    "level_4": { "avg": 380, "range": [200, 500], "samples": 12 }
  }
}
```

#### 5.2 按章节位置统计

识别论文不同部分的字数特征:

```json
{
  "by_position": {
    "introduction": { "chapters": ["1"], "avg": 900, "range": [800, 1000] },
    "analysis": { "chapters": ["2"], "avg": 750, "range": [600, 900] },
    "design": { "chapters": ["3", "4"], "avg": 680, "range": [500, 800] },
    "implementation": { "chapters": ["5"], "avg": 600, "range": [400, 700] },
    "testing": { "chapters": ["6"], "avg": 550, "range": [400, 650] },
    "conclusion": { "chapters": ["7"], "avg": 800, "range": [700, 900] }
  }
}
```

### Phase 6: 术语和风格分析

#### 6.1 专业术语提取

**提取规则**:
- 高频技术术语（出现3次以上）
- 缩写和全称对应
- 特定领域术语

```json
{
  "technical_terms": {
    "RBAC": "基于角色的访问控制",
    "DFD": "数据流图",
    "JWT": "JSON Web Token"
  },
  "high_frequency_terms": [
    "系统架构",
    "数据流",
    "访问控制"
  ]
}
```

#### 6.2 写作风格特征

**识别内容**:
- 图表引用格式
- 技术描述深度
- 段落组织方式

```json
{
  "writing_style": {
    "figure_reference_format": "如图{X}-{Y}所示",
    "table_reference_format": "表{X}-{Y}展示了",
    "technical_detail_level": "moderate",
    "code_inclusion": false,
    "academic_tone": "formal"
  }
}
```

## 📦 输出格式

### 生成文件: `<projectRoot>/paper/reference-structure.json`

```json
{
  "metadata": {
    "source_file": "reference-papers/郭正云.md",
    "parsed_date": "2025-09-30",
    "total_chapters": 54,
    "total_words": 28000
  },

  "chapter_hierarchy": [
    {
      "id": "4.3",
      "title": "系统访问流程",
      "level": 2,
      "parent": "4",
      "has_children": true,
      "has_content": true,
      "has_image": true,
      "word_count": 187,
      "image_info": {
        "type": "dfd",
        "name": "access-flow",
        "ref": "图4-11"
      }
    }
  ],

  "special_patterns": {
    "parent_with_content": [
      {
        "id": "4.3",
        "title": "系统访问流程",
        "keyword_indicators": ["访问", "流程"],
        "has_image": true
      }
    ],

    "implementation_structure": {
      "pattern": "{X}.{Y}.1 功能描述 + {X}.{Y}.2 实现过程",
      "description_suffix": "功能描述",
      "implementation_suffix": "实现过程",
      "description_has_flowchart": true,
      "implementation_has_screenshot": true
    },

    "usecase_structure": {
      "pattern": "{X}.{Y}.{Z} <角色>用例分析",
      "suffix": "用例分析",
      "has_usecase_diagram": true
    },

    "dfd_hierarchy": {
      "top_level_chapter": "4.3.1",
      "has_top_level_diagram": true,
      "decomposition_chapters": ["4.3.1.1", "4.3.1.2"],
      "naming_pattern": "dfd-{module}-level2.png"
    }
  },

  "image_distribution": [
    {
      "chapter_id": "4.3",
      "image_type": "dfd",
      "image_name": "access-flow",
      "path_variable": "${dfd}/access-flow.png"
    }
  ],

  "table_distribution": [
    {
      "chapter_id": "3.2.1",
      "table_name": "users",
      "path_variable": "${tables}/Tab-users.json"
    }
  ],

  "word_count_analysis": {
    "by_level": {
      "level_1": { "avg": 850, "range": [600, 1000], "samples": 6 },
      "level_2": { "avg": 680, "range": [400, 900], "samples": 18 },
      "level_3": { "avg": 520, "range": [300, 700], "samples": 32 },
      "level_4": { "avg": 380, "range": [200, 500], "samples": 12 }
    },
    "by_position": {
      "introduction": { "chapters": ["1"], "avg": 900 },
      "design": { "chapters": ["3", "4"], "avg": 680 },
      "implementation": { "chapters": ["5"], "avg": 600 }
    }
  },

  "terminology": {
    "technical_terms": {
      "RBAC": "基于角色的访问控制",
      "DFD": "数据流图"
    },
    "high_frequency_terms": [
      "系统架构",
      "数据流"
    ]
  },

  "writing_style": {
    "figure_reference_format": "如图{X}-{Y}所示",
    "table_reference_format": "表{X}-{Y}展示了",
    "technical_detail_level": "moderate",
    "code_inclusion": false,
    "academic_tone": "formal"
  }
}
```

## 🔗 与其他工作流的集成

### 1. Workflow 02: 大纲确认与生成

**新增模式D**: 基于参考论文生成大纲
- 扫描 `reference-papers/*.md`
- 执行本工作流生成 `reference-structure.json`
- 基于参考结构直接生成 `outline.json`

### 2. Workflow 03: 基于大纲预填内容计划

**规则优先级**:
```
IF paper/reference-structure.json 存在:
    参考论文规则 (最高优先级)
        ↓
    关键词匹配规则
        ↓
    默认规则
ELSE:
    关键词匹配规则
        ↓
    默认规则
```

**具体应用**:
- **父章节内容规则**: 匹配 `special_patterns.parent_with_content`
- **图表类型选择**: 参考 `image_distribution` 和 `special_patterns`
- **命名模式**: 遵循 `implementation_structure`、`usecase_structure` 等模式
- **字数分配**: 参考 `word_count_analysis`

### 3. Workflow 05.1: 智能内容生成

**加载参考风格**:
```
IF paper/reference-structure.json 存在:
    加载 terminology 和 writing_style
    使用参考论文的术语和表达方式
ELSE:
    使用默认学术写作风格
```

### 4. Workflow 07: UML图表生成

**参考图表规范**:
```
IF paper/reference-structure.json 存在:
    参考 image_distribution 的命名规范
    匹配 special_patterns 中的图表模式
ELSE:
    使用默认命名和布局
```

## 🛠 执行示例

### 完整执行命令

```bash
# Step 1: 扫描参考论文目录
Glob({
  "pattern": "*.md",
  "path": "<projectRoot>/reference-papers"
})

# Step 2: 读取参考论文（假设选择第一个）
Read({
  "file_path": "<projectRoot>/reference-papers/郭正云.md"
})

# Step 3: AI 解析结构
# AI 基于本文档规则自动解析 Markdown 内容

# Step 4: 生成结构文件
Write({
  "file_path": "<projectRoot>/paper/reference-structure.json",
  "content": "{ /* 完整的 JSON 结构 */ }"
})
```

## 📊 质量检查

### 解析完整性
- ✅ 所有章节层级正确提取
- ✅ 图表引用全部识别
- ✅ 特殊模式准确识别
- ✅ 字数统计合理
- ✅ JSON 格式正确

### 规则有效性
- ✅ 父章节内容规则有明确示例
- ✅ 命名模式可复现
- ✅ 图表类型映射准确
- ✅ 字数范围合理

## 💡 使用建议

### 最佳实践
1. **准备阶段**: 将参考论文 MD 文件放入 `<projectRoot>/reference-papers/`
2. **解析阶段**: 在执行 Workflow 02 之前或选择模式D时执行
3. **审查阶段**: 生成后检查 `reference-structure.json` 的关键规则
4. **应用阶段**: 后续工作流自动加载并应用参考规则

### 适用场景
- ✅ 有类似结构的参考论文
- ✅ 想要保持与参考论文风格一致
- ✅ 需要快速复现成功的论文结构模式
- ⚠️ 参考论文结构差异较大时可能需要人工调整

### 通用性保证
- ✅ **跨项目复用**: 解析逻辑与具体业务无关
- ✅ **向后兼容**: 不影响不使用参考论文的项目
- ✅ **标准化约定**: `reference-papers/` 目录名固定
- ✅ **可选功能**: 用户自主决定是否启用

## 📝 总结

本工作流是通用参考论文集成方案的基础，通过智能解析参考论文的结构、图表、术语和风格，为后续的自动化论文生成提供高质量的参考依据。

**核心价值**:
- 🎯 **通用设计**: 适用于任何毕业论文模板项目
- 🔄 **规则复用**: 将隐性知识转化为显性规则
- 📊 **数据驱动**: 基于实际论文数据指导生成
- 🚀 **效率提升**: 减少手动配置工作

**通用性承诺**:
- 标准化目录约定 (`reference-papers/`)
- 与模板无关的解析逻辑
- 向后兼容的集成方式
- 渐进式功能采用